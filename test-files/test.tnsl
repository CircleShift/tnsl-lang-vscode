:include 'tnsl'
:using 'tnsl'

/:include
	"this"
	'that'
:/

/; if (int i = 0; i < 1)
	;file.open("file_to_open.tnsl")
;; else
	;file_pointer.close()
;/

;{}charp s = "\uffffff"

;string str = string.from_un71(s)
2.0
.20
..0.0..

/; some_looping_func (int i, j) [int]
	/; loop (i > 0) [i--]
		/; if (i % j == 0)
			;break
		;/
	;/
	;return i
;/

# Comments
/# Block comments
#/
/## Doc comments
	{@code Style the documentation!}
#; back_to_this [~void(int, int)[int]]
	;return ~some_looping_func
;/